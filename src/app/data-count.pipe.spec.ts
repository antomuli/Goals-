import { DataCountPipe } from './data-count.pipe';

describe('DataCountPipe', () => {
  it('create an instance', () => {
    const pipe = new DataCountPipe();
    expect(pipe).toBeTruthy();
  });
});
